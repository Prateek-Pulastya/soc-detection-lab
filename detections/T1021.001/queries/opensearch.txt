# Generated by (NOTE: security channel, not the sysmon pipeline):
#   sigma convert -t opensearch ../../sigma/T1021.001_rdp_logon.yml
#   (add a windows-audit / security pipeline if your setup needs field remapping)
#
# EXPECTED SHAPE — regenerate to confirm exact field names for your Wazuh field mapping.
# Wazuh decodes Security events under win.system.eventID / win.eventdata.logonType.

(EventID:4624 AND LogonType:10)

# Wazuh-field variant (if querying the wazuh-alerts index directly):
# (data.win.system.eventID:"4624" AND data.win.eventdata.logonType:"10")
